/**
 * Index file for all enums
 */
const SeatStatus = require('./seat-status.enum');
const TransactionType = require('./transaction-type.enum');
const NodeRole = require('./node-role.enum');
const ElectionStatus = require('./election-status.enum');

module.exports = {
  SeatStatus,
  TransactionType,
  NodeRole,
  ElectionStatus,
};